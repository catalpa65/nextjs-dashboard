/* This is the loading component for the dashboard page. It is used to show a loading state while the dashboard is loading. */
import DashboardSkeleton from "@/app/ui/skeletons";

export default function Loading() {
  return <DashboardSkeleton />;
}
